[pytest]
addopts = -ra
testpaths = tests
python_files = test_*.py
markers =
    firmware: builds firmware with PlatformIO, no device required
    hardware: requires a connected ESP32 and local Bluetooth tooling
