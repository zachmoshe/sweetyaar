[project]
name = "sweetyaar"
version = "0.1.0"
description = "ESP32-WROOM-32 baby toy audio controller firmware and local tooling."
requires-python = ">=3.11"
dependencies = [
    "bleak>=3,<4",
    "platformio==6.1.19",
    "pyserial>=3.5,<4",
    "pytest>=8,<9",
]

[tool.uv]
package = false
